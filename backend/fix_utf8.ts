import User from './src/models/User'; import connectDB from './src/config/db'; import dotenv from 'dotenv'; dotenv.config({override: true}); connectDB().then(async () => { const user = await User.findOne({clerkId: 'user_3CIVJyUvYzNJ0fQ41mKgmHGXrwO'}); console.log('USER:', user); if (user) { user.role = 'student'; user.permissions = ['view:course', 'submit:quiz']; await user.save(); console.log('Role updated to student.'); } process.exit(0); });
